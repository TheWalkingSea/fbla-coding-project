To execute the program, you must open the dist folder and run the main.exe program

To build the program, you must have python and pyinstaller installed. You must the execute `pip install -r requirements.txt` in the prompt in a virtual environment located at /venv to install requirements. Finally, if they are both installed, you can run build.bat and the executable will be in dist/main.exe